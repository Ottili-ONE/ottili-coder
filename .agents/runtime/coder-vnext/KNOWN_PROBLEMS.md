# Known Problems

Entries are current defects, evidence gaps, or externally constrained facts.
They are not waived by a passing component test.

| ID     | Severity | Symptom / impact                                                        | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Status                        | Intended fix                                                                                                               |
| ------ | -------- | ----------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------- | -------------------------------------------------------------------------------------------------------------------------- |
| KP-001 | low      | Claude Code archive unavailable locally                                 | Clone required GitHub authentication; donor lock records reference-only status.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | open, non-blocking            | Retain no-source-reuse rule; do not make it a build dependency.                                                            |
| KP-002 | low      | Ottili ONE platform source unavailable locally                          | Clone required GitHub authentication; donor lock records optional status.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | open, non-blocking            | Keep local mocks/interfaces testable.                                                                                      |
| KP-003 | medium   | System Node cannot reliably run installed pnpm toolchain                | System Node is v20; supported local Node is `/opt/node24/bin/node` v24.19.0.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | mitigated                     | Pin/use Node 24 in local commands and CI.                                                                                  |
| KP-004 | high     | Lease takeover fencing is only partially verified                       | All 81 public `RunStore` mutators were audited; 11 unfenced executor writes were fixed (migration 4 fences resource locks by generation). `tests/recovery/competing-daemon-takeover.test.ts` proves 18 writes reject a superseded lease and a daemon killed mid-provider-call cannot commit on wakeup; `tests/e2e/daemon-kill-mission.test.ts` proves the same under a real `SIGKILL`.                                                                                                                                                                                                                                                                                                                                      | resolved                      | Preserve both regressions; re-run after any new executor-owned write is added.                                             |
| KP-005 | high     | Durable completion-gate bypass                                          | Direct Store regression now rejects the former bypass; Store rechecks requirements, validation, and verifier acceptance.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | resolved                      | Preserve regression and exercise all reachable HTTP/runtime boundaries.                                                    |
| KP-006 | medium   | Context-overflow handoff was absent                                     | Current coordinator integration persists a `ContextSnapshot` and supplies it to the next epoch.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | resolved                      | Retain regression; broader context composition is KP-012.                                                                  |
| KP-007 | medium   | Durable-entity projections need broader end-to-end proof                | Store projection/v1 upgrade tests and approval API→SDK→CLI resolution pass; full entity API/SDK restart coverage is incomplete.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | open                          | Complete endpoint/SDK and restart/roundtrip evidence.                                                                      |
| KP-008 | medium   | Root validation was failing                                             | Final frozen matrix passes install, lint, format, typecheck, 69/69 unit, 14/14 integration, recovery/e2e, boundaries, build, package, bench, and diff check.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | resolved                      | Preserve green matrix in CI/final evidence.                                                                                |
| KP-009 | medium   | Cooperative shutdown/SSE safety was unproven                            | Lifecycle integration proves provider drain before DB close and active SSE exit before HTTP shutdown.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | resolved (cooperative scope)  | Retain regression; non-cooperative executor boundary is KP-018.                                                            |
| KP-010 | low      | OCF benchmark evidence is narrower than required                        | Benchmark runs but lacks full representative dataset/tokenizer matrix.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | open                          | Extend benchmark and record reproducible results.                                                                          |
| KP-011 | medium   | Bundled-daemon verifier boundary lacks end-to-end provider proof        | `tests/e2e/daemon-kill-mission.test.ts` drives a real bundled daemon through a real HTTP-shaped BYOK provider to a completion the deterministic verifier and completion gate both accept, past a `SIGKILL` and daemon takeover.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | resolved                      | Preserve the regression; add a second scenario if a configured (non-default) verifier is built.                            |
| KP-012 | medium   | Long-horizon context is bounded and incompletely composed               | `RunContextCompiler` (packages/runtime/src/context.ts) composes mission/goal/task/requirement state, task graph, agent inbox, failing validations, checkpoint summary, memory, open problems, Git status/diff, LSP diagnostics (via an injectable port), semantic search, and the repo map through the context planner's token budget; omissions are recorded as a durable `context.compacted` event. `tests/integration/context-composition.test.ts` proves live-workspace composition, budget-forced omission, and graceful degradation without a checked-out workspace.                                                                                                                                                  | resolved                      | Extend the `DiagnosticsProvider` port to a real LSP client when one is composed into the daemon.                           |
| KP-013 | medium   | Sandbox semantics rely on declared tool contracts and lack OS isolation | Fixed a scope-matching defect where a tool's resource scope was namespaced with `${workspaceUri}:${identifier}` before policy evaluation, so no sandbox `writableRoots` entry could ever match and every workspace write silently required approval; scopes are now joined path-shaped and evaluated post-namespacing (`tests/e2e/daemon-kill-mission.test.ts` exercises the fixed path end-to-end). MCP tools now flow through this same scope/policy pipeline (`packages/runtime/src/mcp-tools.ts`, `tests/integration/mcp-lsp-composition.test.ts`). Tool metadata is still trusted and no OS-native sandbox exists.                                                                                                     | open                          | Harden/validate tool metadata and document or provide an OS isolation boundary.                                            |
| KP-014 | medium   | Pause/cancel abort propagation was unproven                             | Lifecycle integration proves pause aborts a blocking provider without retry and cancel aborts a delayed tool without another provider turn.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | resolved (cooperative scope)  | Retain regression; non-cooperative executor boundary is KP-018.                                                            |
| KP-015 | medium   | Some capability primitives remain outside live daemon flows             | Locks, tool usage/cost, bounded history, the live context compiler (KP-012), stagnation response, MCP/LSP (KP-013), isolated worktrees for delegated agents, and now checkpoints are all wired into the coordinator with direct regressions: `createMcpTools`/`LspServerManager`/`GitWorktreeProvisioner`/`createMilestoneCheckpoint` (`packages/runtime/src/mcp-tools.ts`, `lsp-tools.ts`, `worktrees.ts`, `coordinator.ts`) compose into the daemon's tool registry/context port/turn workspace/checkpoint pipeline behind opt-in config, proven end-to-end by `tests/integration/mcp-lsp-composition.test.ts`, `tests/integration/worktree-composition.test.ts`, and `tests/integration/checkpoint-composition.test.ts`. | resolved                      | Preserve all four regressions; the durable `checkpoints` list API/SDK/CLI (previously always empty) now returns real data. |
| KP-016 | medium   | Standard Node CLI installation was previously unproven                  | Current bundled CLI package smoke passes, but release/publish compatibility is not audited.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | mitigated, partially verified | Add clean published-package/install coverage before release.                                                               |
| KP-017 | low      | PID reuse is mitigated but retains a narrow check-to-signal race        | Stop refuses legacy descriptors and verifies endpoint `instanceId` before signaling; process identity cannot be atomically bound to `SIGTERM`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | mitigated, partially verified | Preserve regression; use a stronger OS/process identity primitive if the residual race matters.                            |
| KP-018 | medium   | A third-party executor can deliberately hold graceful shutdown          | Scheduler safely waits for active execution; a provider/tool that ignores `AbortSignal` and never settles can hold shutdown, but cannot cause DB-close writes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | open                          | Define/enforce a bounded executor contract or watchdog/escalation policy.                                                  |

| KP-019 | high | macOS worktree creation failed on symlinked workspace roots | GitHub Actions macOS job raised `GitSnapshotError: Git created a worktree but it could not be listed afterward`; reproduced locally through a symlinked temp root. | resolved | `canonicalizePath`/`WorktreeManager.find` compare filesystem locations; symlinked-workspace regression added. |
| KP-020 | high | Windows checkout produced CRLF and failed `prettier --check` wholesale | GitHub Actions Windows job failed formatting on effectively every file. | resolved | `.gitattributes` pins LF, `.prettierrc.json` states the policy, and `pnpm check:eol` fails first with a named cause. |
| KP-021 | high | `daemon start`/`daemon stop` could not work on Windows | Start used a POSIX-only `exec` through a shell; stop relied on `SIGTERM`, which Node maps to `TerminateProcess` on Windows so no handler runs. | resolved | Bundled daemon spawns argv-only with no shell; `POST /v1/daemon/shutdown` stops it cooperatively with a signal fallback. |
| KP-022 | medium | `execute_command` could not run `.cmd`/`.bat` targets on Windows | Node refuses to spawn batch files without a shell, so `npm`/`pnpm` were unrunnable. | resolved | `PATHEXT` resolution plus `cmd.exe /d /s /c` routing under a metacharacter-refusal contract (ADR-008). |
| KP-023 | high | Cross-platform CI needs re-verification on the final commit | GitHub Actions run 32132026366 (commit `3f51516`) passed all three platforms; five later runs each surfaced one real defect (KP-025 lease TTL/EBUSY, KP-026 Windows drive-letter-as-URL, KP-027 SQLite transient IOERR, KP-028 the same drive-letter defect in LSP config) before run 32231877726 (commit `9a5f310`, 2026-08-19) passed Ubuntu, macOS, and Windows together. | resolved | Re-confirm on every subsequent substantive change; do not treat the local matrix alone as sufficient platform evidence. |
| KP-025 | high | A short-TTL `RunScheduler` lease can expire mid-turn on a loaded CI runner, even with its heartbeat running | `tests/integration/multi-agent-graph.test.ts`'s restart test used a 40 ms lease TTL (13 ms heartbeat interval) to force a fast crash simulation. On Windows CI this let the lease actually expire mid-`plan_tasks` call: `store.createTask` for the third task hit `LeaseFencedError`, which is swallowed as an ordinary tool failure inside the turn and then again (silently) by the scheduler's own `LeaseFencedError` handling, so the task was dropped without the test or the scheduler reporting an error. A parallel Windows-only `EBUSY` failure on SQLite temp-directory cleanup (`fs.rm`'s `maxRetries` defaults to 0) compounded the same run. | resolved | Raised the TTL to 500 ms (600 ms crash-simulation sleep) — 60x the CI-observed stall, still under a second of wall clock. Added `tests/support/fs-cleanup.ts` (`removeTempDirectory`, `maxRetries: 10`) and switched every test's temp-directory teardown to it. |

`KP-004` remains high-risk despite targeted remediation. Every open item and
every `UNPROVEN` ledger requirement prevents `TRUE_COMPLETE`.

| KP-024 | medium | `packages/control-plane/src/store.ts` is a single ~3000-line module | 81 public methods span missions/runs/goals/tasks/agents/leases/locks/budgets/checkpoints/context/memory/requirements/validation/recovery in one file, which slows review and increases merge-conflict risk. | open | Decompose into focused modules under `src/store/` without changing transactional behavior; see NEXT_ACTIONS.md. |
| KP-026 | high | A Windows absolute `--workspace` path was silently redirected to the daemon's own working directory | `new URL("C:\\Users\\x")` succeeds with `protocol` `"c:"` — a single letter followed by `:` is syntactically a valid URL scheme — so `apps/cli/src/commands.ts`'s `workspaceUri()` treated any Windows drive-letter path as an already-formed URL instead of a filesystem path. The resulting non-`file:` "URI" failed every downstream `startsWith("file:")` check, so `daemon-process.ts`'s `workspacePath()` silently fell back to `process.cwd()` — the daemon acted on its own checkout, not the requested workspace, while the CLI reported success. First caught by `tests/e2e/daemon-kill-mission.test.ts` on Windows CI (`Could not find 'packages/money/test/discount.test.mjs'`, since that path does not exist in the product repo). | resolved | `WINDOWS_DRIVE_PATH` regex routes drive-letter paths to the path branch before URL parsing is attempted (ADR-014). Regression: `tests/unit/cli.test.ts` "treats a Windows drive-letter path as a filesystem path, not a URL". |
| KP-027 | high | Opening a fresh SQLite connection right after a killed process held the same file could fail with a transient `disk I/O error` on Windows | GitHub Actions Windows run for KP-026 failed `pnpm test:e2e` with `ERR_SQLITE_ERROR errcode 1546` (`SQLITE_IOERR_TRUNCATE`) when the daemon-kill mission opened a fresh `RunStore`/`SqliteDatabase` immediately after confirming the first (SIGKILLed) daemon process had exited. A process killed with `SIGKILL` never calls `close()`, so Windows can hold the file's OS-level handle for a brief window after the process has already exited — this is a real production risk for any Windows daemon restart after a crash, not just the test. | resolved | `SqliteDatabase`'s constructor retries the open (bounded, exponential backoff via a synchronous `Atomics.wait` sleep, since `node:sqlite` is synchronous throughout) for the `SQLITE_IOERR`/`BUSY`/`LOCKED`/`CANTOPEN` result-code family only; `isTransientOpenError` is unit tested against the exact CI-observed code and against non-transient errors that must not be masked. |
| KP-028 | medium | `assertAbsoluteUri` (LSP config) accepted a Windows drive-letter path as a valid URI | Same root cause as KP-026, found proactively while auditing for the same pattern: `new URL("C:\\project").protocol` is `"c:"` — non-empty — so a check for "protocol is non-empty" incorrectly accepted a bare Windows path for `rootUri`/`workspaceFolders[].uri`. LSP is not yet composed into the live runtime (KP-013/KP-015), so this had no current production path, but would have broken silently the moment it was wired up on Windows. | resolved (fix now load-bearing: LSP is composed into the live runtime as of this session) | `assertAbsoluteUri` now rejects a Windows drive-letter path explicitly before attempting `new URL(...)`, the same guard as ADR-015. Regression: `tests/unit/mcp-lsp.test.ts` "rejects a Windows drive-letter path as a rootUri or workspace folder URI". |
| KP-029 | medium | Two new MCP/LSP composition test fixtures built a `file://` URI by string concatenation, the same defect class as KP-026/KP-028 — but in the tests, not the product | GitHub Actions Windows run for the MCP/LSP composition commit failed `pnpm test`: `tests/unit/mcp-lsp-tools.test.ts` asserted the LSP tool would open `` `file://${workspace}/money.ts` `` (raw concatenation) but the product correctly opened `pathToFileURL(...).href`'s properly percent-encoded form. Auditing the sibling `tests/integration/mcp-lsp-composition.test.ts` (which never reached CI, since the failed unit-test step aborted the Windows job first) found the same pattern building `workspaceUri`, and confirmed by direct `new URL(...)` reproduction that on Windows `` `file://${rawDrivePath}` `` has too few slashes and is misread as an authority component (`file://D:\x` → host `"D:"`), the same root cause as ADR-015/ADR-016 — `RunContextCompiler.workspacePathOf` parses `mission.workspaceUri` with the real `fileURLToPath`, so this would have silently broken the "next turn's compiled context" assertion on Windows the first time that test actually ran there. | resolved | Both fixtures now build the URI with `pathToFileURL(path).href` and recover the raw path with `fileURLToPath(uri)`, matching the product's own pattern exactly, instead of string concatenation. |
| KP-030 | medium | A second short-lease-TTL test raced `RunCoordinator.execute`'s own first lease check, the same class of flake as KP-025 — this time on Ubuntu, in a different test | GitHub Actions Ubuntu run for the KP-029 fix failed `pnpm test:recovery`: `competing-daemon-takeover.test.ts`'s "lets a successor finish work a killed daemon started" timed out at 15000 ms. Root cause: `RunCoordinator.execute` checks the lease via `recoverGraphWork` _before_ ever calling the provider, so a 40 ms TTL raced that very first check on a loaded CI runner; when the check lost the race, `execute()` rejected with `LeaseFencedError` before the provider was ever invoked, and the test's `await held.started` (which only resolves once the provider is called) hung forever, surfacing only as the outer 15 s test timeout plus an "unhandled rejection" once Vitest gave up. | resolved | Raised the stale lease to `ttlMs: 500` and its paired `setTimeout` wait to 600 ms — the same margin already established for KP-025 — so `execute()` reliably reaches the provider before the lease legitimately expires during the wait. Confirmed stable across 3 consecutive local runs. |
| KP-031 | medium | A sandbox's `writableRoots` does not automatically widen to cover a dynamically-provisioned Agent worktree, and even an operator who grants the parent directory can still be silently denied by a canonicalization mismatch | `GitWorktreeProvisioner` places a delegate's worktree as a sibling of the primary workspace (`<parent>/.ottili-worktrees/<runId>/<agentId>`), a path no `writableRoots` entry configured only with the primary workspace's URI (the CLI's own default, `apps/cli/src/commands.ts`) would cover. Confirmed on real CI, not just reasoned about: `tests/integration/worktree-composition.test.ts` initially granted the fixture's `parent` directory explicitly and still failed on macOS and Windows (Ubuntu passed) — the delegate's write was silently denied (a policy `deny`, not a crash; the turn ended normally on a later text step with no visible error) because the sandbox's `writableRoots` entry was built from the _raw_ `os.tmpdir()` path while `WorktreeManager` always reports a worktree's path the way Git does, canonical (`/private/var/...` on macOS, the long-name form of a short-name temp path on Windows) — the same root-cause class as ADR-009/KP-019/KP-029, reached this time through a sandbox `writableRoots` comparison instead of a worktree-listing or URI-construction comparison. | resolved | `RunCoordinator.sandboxForTurn` (ADR-020) grants a delegate's own `worktreeUri` as an additional, ephemeral `writableRoots` entry for that turn's authorization checks only — never persisted, and never visible to `createMissionTools`, so it cannot leak into a grandchild's durable sandbox via `delegate_task`. An operator now configures `writableRoots` exactly as the CLI's own default already does (the primary workspace only); no pre-configuration for worktrees is needed. Regression: `tests/integration/worktree-composition.test.ts` was deliberately narrowed to grant only the primary workspace (removing the `parent`-directory workaround) and still passes, proving the delegate's write succeeds solely because of the automatic per-turn grant. |
| KP-032 | low | A `LeaseFencedError` was observed once from `RunStore.recoverClaimedWork` on a lease acquired moments earlier with a 60 s TTL, in a run that touched no source related to leasing | GitHub Actions Ubuntu job for a WUID-notes-only commit (`9a43323`, zero source changes) failed `multi-agent-graph.test.ts`'s "reconstructs task readiness, ownership, and the agent graph after a daemon restart": `RunScheduler.tickRun` threw `LeaseFencedError` from `recoverClaimedWork`, called with the lease `acquireLease` had just returned in the same synchronous call chain (`leaseTtlMs: 60_000`) — too generous a margin for the usual short-TTL-race explanation (KP-025/KP-030's root cause) to apply directly. Re-ran on the very next commit (`5609b69`, checkpoint composition) and all three platforms passed, including this same test. | open, monitoring | Not reproduced on demand; no code change made without stronger evidence. If this recurs, capture the full job log (lease table state, exact timestamps) before attempting a fix — do not guess a TTL bump the way the first KP-030 attempt did. |
| KP-033 | low | The protocol's `Checkpoint`/`CheckpointListResponse` types do not match what the `GET /v1/runs/:id/checkpoints` endpoint actually returns | `packages/protocol/src/contracts.ts`'s `Checkpoint` interface declares `workspaceRevision`, `contextSnapshotId`, and `artifactIds` fields; `RunStore.listCheckpoints`/`getCheckpoint` actually return `CheckpointRecord` (`label`, `reason`, `manifest`, `workspaceRef`) — a structurally different shape, pre-existing before this session's checkpoint composition work. `server.ts`'s checkpoints route responds with the real `CheckpointRecord[]` typed loosely enough that `tsc` does not catch the mismatch; found while building the checkpoint-restore feature and cross-checking the existing GET route against the protocol contract. | open | Reconcile `Checkpoint`'s declared shape with `CheckpointRecord`'s actual one (or formally split them if they are meant to diverge) alongside the R53 full-endpoint-coverage work; low severity since the SDK/CLI already consume the real shape correctly by not asserting the aspirational one. |
| KP-034 | low | `packages/integrations/src/provider.ts`'s `OttiliAiAdapter`/`ManagedAuthAdapter`/`OpenAiCompatibleAdapter`/`ProviderHttpError` — R45's named subject — were unused anywhere in the product, and structurally duplicated a different, live implementation elsewhere | Grepped all `packages/*/src`/`apps/*/src`/`tests/*` files for any import of these four exports outside their own file (zero matches), confirmed the package is `"private": true` (no external-consumer semver risk), and confirmed the real, live Ottili-managed-provider path is `packages/runtime/src/provider-registry.ts`'s `ottili` case — `ManagedTokenTurnProvider` plus a `CreateProviderOptions.ottiliAccessToken` token-supplier callback, now with full round-trip test coverage (`tests/unit/providers.test.ts`, ADR-022). | resolved | Deleted `packages/integrations/src/provider.ts` and its `export * from "./provider.js"` in `packages/integrations/src/index.ts`; typecheck/lint/boundaries/unit suite all pass with zero remaining references. R45/R47 re-scoped against `provider-registry.ts`'s actual `ottili` path (ADR-022, now PROVEN). |
| KP-035 | low | `LocalExecutionBackend` (`packages/integrations/src/backend.ts`) is not the live implementation `execute_command` (`packages/runtime/src/builtins.ts`) dispatches through, so R48 ("Local execution backend works") cannot honestly move to PROVEN despite the abstraction now having full contract-test coverage | `execute_command`'s own `execute()` helper is materially more hardened than `LocalExecutionBackend.execute`: it resolves Windows batch/PATHEXT targets via `command-target.ts`, incrementally truncates output to `maxOutputBytes`, and fixes an abort-registration race, none of which `LocalExecutionBackend` has. Swapping the call site to the weaker implementation would regress Windows CI and unbounded-output safety. Porting the hardening into `LocalExecutionBackend` first is blocked by `scripts/check-boundaries.mjs`: `integrations` may depend only on `@ottili/core`/`@ottili/protocol`, not `@ottili/runtime`, so `command-target.ts` cannot be imported where `backend.ts` lives without first relocating shared process-exec helpers to a lower package. Found while proving R48/R49 (ADR-022). | open | Deliberately deferred, not a regression: needs a dependency-graph decision (e.g. a new low-level `@ottili/process-exec`-style package both `integrations` and `runtime` can depend on) before `LocalExecutionBackend` can safely become `execute_command`'s real implementation. Track alongside `KP-024`'s module-boundary cleanup rather than as an isolated one-off. |
| KP-036 | low | `action.yml`'s `actions/setup-node@v4` step failed with "Invalid pattern '…/.//pnpm-lock.yaml'. Relative pathing '.' and '..' is not allowed." on the first real `action-smoke` CI run | `cache-dependency-path: ${{ github.action_path }}/pnpm-lock.yaml` string-concatenated a path containing a literal `.` segment: `github.action_path` resolves to exactly `.` for the self-referential `uses: ./` case `action-smoke` exercises, and `setup-node` explicitly rejects any resolved cache-dependency-path containing a `.`/`..` segment. Found directly from the job log (`gh api .../jobs/:id/logs`), not guessed — the failure cascaded and skipped every later composite step, which is exactly the class of defect the `action-smoke` smoke test exists to catch. | resolved | Dropped `cache: pnpm`/`cache-dependency-path` from the `setup-node` step entirely rather than trying to normalize the path — an uncached `pnpm install` is a small, predictable cost next to a whole class of path-normalization bugs, and a real consumer invocation is a cold VM either way. Fixed on the same commit range as `action.yml`'s introduction, before the next CI push; re-confirmed with direct evidence on run 32294770672 — `action-smoke` job green, internal log shows the full sequence ran for real (build, daemon start, Run creation, poll, correct `waiting_external` detection, daemon stop). |
